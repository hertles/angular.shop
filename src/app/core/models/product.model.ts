export interface ProductModel {
  id: number
  title: string
  price: number
  description: string
  category: string
  image: string
  rating: {
    rate: number
    count: number
  }
}

export interface ChosenProduct extends ProductModel {
  chosen?: boolean
}
